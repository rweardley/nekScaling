# turbulentPipe

This case demonstrates turbulent simulations in NekRS. An LES (Large-Eddy Simulation) model known in NekRS as `hpfrt` is used.

The case should resolve the turbulence reasonably well, and should probably be used for strong scaling rather than weak scaling.